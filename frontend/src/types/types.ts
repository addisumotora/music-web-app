export interface User {
    id?: string;
    name?: string;
    email: string;
    password: string;
}

export interface Music {
    id?: string;
    title: string;
    album: string;
    genre: string;
    image: string;
    artist: string;
}